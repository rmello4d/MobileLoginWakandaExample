import { Component } from '@angular/core';import { NavParams, ViewController } from 'ionic-angular';@Component({  selector: 'page-ocr',  templateUrl: 'ocr.html'})export class OcrPage {  info: any;  phrase: string;  text: string;  constructor(    public viewCtrl: ViewController,    params: NavParams  ) {        this.info = JSON.parse(params.get('results'));    this.text = '';    this.phrase = '';    var that = this;    if (this.info.regions) {    	this.info.regions.forEach(function(region){    		if (region.lines){    			region.lines.forEach(function(line){    				if (line.words) {    					line.words.forEach(function(word){    						if (word)    							that.text += that.text + word.text + ' ';    					})    				}    				    			})    			    		}    		    	})  	    }    this.phrase = this.text;      }  dismiss() {    this.viewCtrl.dismiss();  }}